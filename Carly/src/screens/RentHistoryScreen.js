import { View, Text, StyleSheet } from 'react-native';
import { styles } from '../styles';

export default function RentHistoryScreen() {
	return (
		<View style={styles.screenContainer}>
			<Text>Rent History Screen</Text>
		</View>
	);
}
